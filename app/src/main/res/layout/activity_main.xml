<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:gravity="center">

    <GridView
        android:layout_width="wrap_content"
        android:layout_height="350dp"
        android:id="@+id/board"
        android:numColumns="3"
        android:choiceMode="none"
        android:gravity="center"
        android:layout_marginBottom="25dp"
        android:layout_above="@+id/floatingActionButton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:layout_marginTop="60dp"
        android:id="@+id/title"
        android:gravity="center"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/floatingActionButton"
        android:layout_alignEnd="@+id/floatingActionButton" />

    <applicotest.android.com.android_tic_tac_toe.animation.FloatingActionButton
        android:id="@+id/floatingActionButton"
        android:layout_width="74dp"
        android:layout_height="74dp"
        android:layout_gravity="right|bottom"
        app:actionColor="?attr/colorAccent"
        app:actionIcon="@drawable/ic_refresh_white_24dp"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/board"
        android:layout_alignEnd="@+id/board"
        android:onClick="restartButtonOnClick" />

</RelativeLayout>
